# 归档 Demo

这里保存第 21 到第 53 课的原始、可独立运行快照。每个脚本需要的 Markdown 和 JSON 索引也在本目录，因此可以直接在此目录运行旧课件。后续每完成一个新课程模块，都会同步在这里保留对应编号的独立案例。

新的学习主线使用 `../lessons/`，实际持续维护的程序使用 `../app/聊天问答/rag_cli.py`。归档代码不再作为后续课程的主要编辑对象。
