# 当前学习状态

## 当前位置

当前处于 **RAG 工程闭环** 阶段：已经能检索、过滤、重排、缓存，也能在修改 Markdown 知识库后重新构建应用实际使用的索引。

当前完整流程是：

```text
修改 data/source/长文档示例.md
-> 运行 app/知识库构建/build_parent_child_index.py 重建索引
-> 索引 hash 自动变化，旧检索缓存自然失效
-> 运行 app/质量检查/evaluate_retrieval.py 检查检索结果
-> 重启 app/聊天问答/rag_cli.py，用新资料回答用户
```

## 已完成

- 基础对话、消息历史、提示词、JSON 输出与工具调用。
- 普通 RAG：切块、向量索引、语义检索与相似度阈值。
- 长文档 RAG：`chunk_overlap`、父子切块与父章节回收。
- 第 30 课：父子知识库终端连续查询。
- 第 31 课：父子知识库相似度阈值，资料不相关时不调用模型。
- 第 32 课：为回答标注资料来源，展示命中的父章节、子片段和相似度。
- 第 33 课：保存对话历史，同时保持每轮重新检索知识库。
- 第 34 课：根据对话历史改写不完整追问，再进行知识库检索。
- 第 35 课：用固定测试题检查检索是否找对章节，以及无关问题是否被拒答。
- 第 36 课：用商品类型等元数据先筛资料，再进行相似度检索。
- 第 37 课：BGE 先召回候选片段，再用 reranker 重新排序。
- 第 38 课：把 BGE 语义检索和 BM25 关键词检索合并，用 RRF 按名次给资料排序。
- 第 39 课：找回完整父章节后，在章节内部选择最相关的句子，减少交给 AI 的无关资料。
- 第 40 课：先把问题路由到合适的知识库主题，再在这个范围内进行 BGE 检索。
- 第 41 课：用固定测试题检查路由是否正确，并处理无路由拒答和多路由范围合并。
- 第 42 课：把多道路由测试题放进列表，批量检查预期路由和实际路由是否一致。
- 第 43 课：用内存字典缓存完全相同问题的检索结果，避免重复调用 BGE。
- 第 44 课：把知识库版本放进缓存 key，资料更新后让旧缓存自然失效。
- 第 45 课：用 TTL 限制缓存有效时间，缓存保存太久后必须重新检索。
- 第 46 课：用 LRU 限制缓存容量，缓存满时删除最久未使用的结果。
- 第 47 课：把内存缓存保存到 JSON 文件，程序重启后从文件恢复缓存。
- 第 48 课：把用户权限放进缓存 key，避免不同用户共用不该看的缓存资料。
- 第 49 课：把权限 key、TTL 和 LRU 容量规则封装成可复用的 RetrievalCache 对象。
- 第 50 课：把 RetrievalCache 接入完整 RAG 检索流程，重复问题命中缓存时跳过 BGE。
- 第 51 课：根据父子索引 JSON 的 SHA-256 自动生成缓存版本，索引变化时旧缓存自然失效。
- 第 52 课：让 rag_cli.py 在启动时读取索引 hash，自动生成检索缓存版本。
- 第 53 课：修改原始 Markdown 后重建当前应用实际使用的父子索引；构建前检查元数据编号，索引 hash 变化后自动切换缓存版本。

## 当前入口

- 完整应用：`app/聊天问答/rag_cli.py`
- 课程模块：`lessons/`
- 历史快照：`archive/`

先运行模块 Demo 验证检索，再回到完整应用观察同一能力如何参与最终回答。

## 下一步

- **第 54 课：检索回归测试。** 知识库每次重建后，自动重新跑固定题，检查新资料没有弄坏旧问题的召回。

## 后续学习路线

不跳课，按下面顺序继续：

```text
阶段 1：RAG 工程闭环（当前）
检索质量测试、资料更新、索引重建、缓存版本。

阶段 2：工具与固定 workflow
先学 Python/API 工具的输入输出；再由程序用固定规则决定调用知识库、订单或工单工具。

阶段 3：单 Agent
让模型根据问题选择工具、决定是否继续查询，并保留任务状态。

阶段 4：harness 工程
给 Agent 加权限、工具范围、重试次数、日志、评测、成本和时间限制。

阶段 5：多 Agent（按业务需要再学）
只有一个 Agent 和固定 workflow 已经无法处理复杂分工时，才学习多 Agent 编排。
```

每个新能力第一次出现时，先在对应 `lessons/.../demo.py` 写完整原始逻辑和实际变量值；后续复用时才放进 `app/`。每课同步保留一份 `archive/` 独立案例。

从第 53 课开始，Demo 专属的 Markdown、JSON 和输出都放在各自的 `lessons/本课目录/资料/`、`输出/` 中；`data/` 只保存完整应用共用的真实资料。

课程 `lesson.md` 默认只写“改哪个文件 -> 运行什么命令 -> 看哪个结果”三步。复杂原理和代码细节只在需要时、用户主动查看时再解释。
